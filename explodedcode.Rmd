---
title: "Exploded code"
subtitle: "Using flipbookr and xaringan"
author: "Me"
output:
  xaringan::moon_reader:
    lib_dir: libs
    css: [default, hygge, ninjutsu]
    nature:
      ratio: 16:9
      highlightStyle: github
      highlightLines: true
      countIncrementalSlides: false
---
---

`r flipbookr::chunk_reveal("name_1", left_assign = "detect")`

---

```{r name_1, include = FALSE}
library(tidyverse)
```

---

`r flipbookr::chunk_reveal("name_2", left_assign = "detect")`

---

```{r name_2, include = FALSE}
ggplot(data = cars) +
  aes(x = dist) +
  aes(y = speed) +
  geom_point()
```

---

`r flipbookr::chunk_reveal("name_3", left_assign = "detect")`

---

```{r name_3, include = FALSE}
cars %>%
  count(speed) %>%
  arrange(-n)
```

---

`r flipbookr::chunk_reveal("name_4", left_assign = "detect")`

---

```{r name_4, include = FALSE}
# hola po
df <- cars %>%
  mutate(id = 1:n())

```
